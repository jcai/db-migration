#!/bin/bash
MQ_MESSAGE="MQ Server"
MQ_DAEMON_PID=mq.server
MQ_MAIN=mq.api.app.MqServer

# Only follow symlinks if readlink supports it
if readlink -f "$0" > /dev/null 2>&1
then
    MQ_BIN=`readlink -f "$0"`
else
    MQ_BIN="$0"
fi
MQ_BIN_DIR=`dirname "$MQ_BIN"`

. "$MQ_BIN_DIR"/mq-daemon.sh 


